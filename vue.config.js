module.exports = {
  // configureWebpack: {
  //   resolve: {
  //     alias: {
  //       components: require('path').join(__dirname, 'src/components')
  //     }
  //   }
  // }
  // configureWebpack (config) {
  //   config.resolve.alias.comps11 = require('path').join(__dirname, 'src/components')
  // }
  // chainWebpack (config) {
  //   // config.resolve.alias.comps = require('path').join(__dirname, 'src/components')
  //   // console.log(config.resolve.alias.store)
  //   config.resolve.alias.store.set('comps', require('path').join(__dirname, 'src/components'))
  //   config.resolve.extensions.add('.suyndada')
  // }
}
